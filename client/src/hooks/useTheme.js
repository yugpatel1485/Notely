/**
 * useTheme.js  (Phase 4)
 * Re-exports the hook from ThemeContext for a cleaner import path.
 *
 * Usage:
 *   import { useTheme } from '../hooks/useTheme';
 *   const { theme, toggleTheme } = useTheme();
 */

export { useTheme } from '../context/ThemeContext';
